# CQ-02：HTTP 与应用用例分离

状态：2026-09-08 本地验收完成；本地任务说明，未发布远程 Issue。

范围覆盖启动依赖、Project/Asset、Quote/Wallet、Generation/Batch/Cancel 四个切片。
用户授权同一对话继续计划全部切片，逐片验证和本地提交。

- `bootstrap.py` 负责 Storage、Provider、WorkflowStarter 和后台入口组装。
- `application_projects/assets/billing/generations.py` 负责业务用例与原有事务提交。
  `application_queries.py` 提供授权读取和共用查询。
- 按业务域划分 HTTP 路由；删除通配兼容导出和 webhook 对 facade 的反向导入。
  下载用例返回对象元数据，响应流与 204 映射留在 HTTP 层。
- 新增 `tests/test_architecture.py`，约束依赖方向、路由不得提交事务/组装账本对象、
  账本与结算不得提交调用者事务。没有削弱业务测试断言。

验收：OpenAPI 导出与原 JSON 语义完全相同，保留原文件字节；定向 PostgreSQL 回归
37 passed（117.26s），`.test-runs/python-n_icryor/python.xml`，覆盖公共契约、权限、
素材绑定、Batch 原子性、幂等、Outbox、webhook、上传；结构检查 3 passed（1.41s），
`.test-runs/python-d_u1ddhx/python.xml`。Ruff 和差异检查在提交前复核。

原生只读预检：PostgreSQL 16.10、Auth 表齐全，开发库停在 0012、代码要求 0015；
未升级开发库。测试由 runner 在已有独立测试库创建隔离 schema 并迁移，不依赖开发库版本。
上传仍同步对象写入（CQ-05/06 处理）；未运行远程 CI。
