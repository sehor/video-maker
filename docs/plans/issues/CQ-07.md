# CQ-07：持续质量门禁与总验收

状态：2026-09-08，本地完成；远程 CI 未运行。

按 CQ-01 基线将依赖方向、HTTP 事务责任、领域事务所有权、执行协作者显式依赖、
async 控制路径不直接打开 Session 固化为 5 项 AST 检查，纳入默认轻量测试。
Ruff 增加迁移、OpenAPI 导出及内存基准脚本；只有历史迁移 0004、0005、0006
原有的 I001 导入排序问题获精确文件例外，没有修改已发布迁移内容，新迁移仍受检查。

总验收发现并修正了两个边界问题：开发充值接口必须继续排除在公共 OpenAPI 外，
因此从实际路由单独生成开发契约；上传和下载只传用户 ID，并在文件传输前关闭认证
与授权 Session。新增连接释放回归，保持原有跨用户权限、结果下载有效性检查。
前端还补足空错误体和响应读取中断处理，保留提交结果未知时的原 operation key。

本地测试、性能记录及远程 CI 状态汇总到[最终验收报告](../../reports/2026-09-08_代码质量验收.md)。
全量初轮 429 passed、1 failed、6 errors；对应失败/错误以及最终改动的定向复核 20 passed，
JUnit 已核对全部 7 项均重新通过。Web 31 passed，E2E 2 passed；全部相关静态检查通过。
没有发布远程 Issue 或推送提交。
