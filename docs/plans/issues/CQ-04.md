# CQ-04：前端请求与 OpenAPI 契约关联

状态：2026-09-08 本地完成；未发布远程 Issue。

所有业务页面、素材分页和生成恢复改用生成的 `paths`：模板路径、参数、query、
HTTP 方法、JSON body 和响应关联推导；删除 `request<T>(string)` 的宽泛兜底。
JSON 在传输边界序列化；FormData、204、认证下载和已有幂等键保持行为。
统一错误包含 status/code/message/requestId/resultUnknown；网络丢失和不确定写入响应
进入原操作恢复，不替换 operation key。恢复控制器每次请求前仍核验当前用户。

唯一预先说明的 OpenAPI 文档扩展：既有 `/v1/wallet/test-grants` 原先隐藏，现纳入
生成契约并注明仅开发使用；生产返回 404 的代码未变。没有手写第二套 DTO。

验证：vue-tsc 包含非法路径/方法/字段、缺失参数、错误响应类型的 `@ts-expect-error`
样例；前端 30 passed（含新增 4 条封装测试）；lint 通过；真实 api-client check 通过。
首轮封装测试复用了已读 Response，改为每次创建 Response 后全部通过，未修改业务断言。
浏览器 E2E 2 passed（20.7s）且生产构建成功，`.test-runs/e2e-cp_x_cq7/`。
未运行远程 CI。
