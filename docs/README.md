# AI 视频镜头工厂开发文档 v2.1

> 日期：2026-08-31｜形式：分阶段、按需加载
> 产品：只做短剧／漫剧视频镜头；只交付 720p／1080p；不做图片、2K／4K／8K和 AI 超分。

## 使用规则

开发时只加载：

```text
00_开发总纲.md + 当前阶段文档
```

修改数据库、状态机、账本或 Provider／Worker 协议时，再加载 `90_核心数据模型与接口契约.md`；新增依赖、模型、节点或 Worker 镜像时，再加载 `91_外部依赖与许可证.md`。不要一次加载全部文件。

默认使用 Windows 原生环境，按根 [README](../README.md) 的 PowerShell 命令开发。
操作本地数据库前加载 `计划二_WSL2_Docker_安装与维护.md` 的原生预检分流，
使用 `scripts/dev.ps1 check` 核对 Windows PostgreSQL，不启动 WSL 或检查容器。
只有明确执行独立 WSL2/Docker/Compose 集成任务时，才进入该文档的容器预检，复用现有资源。

## 2026-09-06 审查后执行计划

以 [项目全面审查报告](reports/2026-09-06_项目全面审查.md) 为依据，新增三份待实施计划：

| 计划 | 范围 | 执行关系 |
|---|---|---|
| [计划一：业务逻辑修复](plans/01_业务逻辑修复计划.md) | F1–F8、任务输入快照、操作幂等、素材与对象生命周期 | 先完成 P1，再做业务总验收 |
| [计划二：代码质量改进](plans/02_代码质量改进计划.md) | 应用服务边界、显式依赖、类型契约、阻塞 IO 和流式存储 | CQ-01 至 CQ-07 本地完成；[验收证据](reports/2026-09-08_代码质量验收.md)，未推送 |
| [计划三：未完成任务推进](plans/03_未完成任务推进计划.md) | 原 #14–#16、真实路线、私测产品和上线验收 | 真实阶段三通过后进入阶段四，阶段五后置 |

上述 BL/CQ/DL 编号是计划任务 ID，不是已创建的 GitHub Issue。按需加载当前计划中的相关任务与前置项，不需要一次读取三份全文。计划文档不代表功能已实现或真实服务已验收。

## 阶段与参考文档

| 文件 | 内容 |
|---|---|
| `00_开发总纲.md` | 产品边界、总架构、全局规则、阶段顺序 |
| `01_阶段一_本地业务闭环.md` | 登录、项目、镜头、上传、Mock 生成 |
| `02_阶段二_秒数账本与可靠任务.md` | Quote、双分录、Outbox、Hatchet、Batch |
| `03_阶段三_RunPod真实视频路线.md` | 首条 720p Worker、媒体校验、Benchmark |
| `04_阶段四_私测上线.md` | 支付、内容策略、Admin、监控、部署 |
| `05_阶段五_多供应商与规模化.md` | Vast、商业 API、New-API、1080p、SkyPilot |
| `90_核心数据模型与接口契约.md` | 表、约束、状态机和统一协议 |
| `91_外部依赖与许可证.md` | 依赖选择、版本锁定和许可证治理 |
| `计划二_WSL2_Docker_安装与维护.md` | 原生数据库预检分流；独立集成／发布的历史容器资源与维护，不是开发前置条件 |
| `Windows原生开发环境改造计划.md` | 移除日常开发的 WSL/Docker 依赖，保留集成与发布容器边界 |
| `runbooks/HatchetCloud_Windows集成.md` | 开发租户凭据、TLS、Windows Worker 与远程验收命令 |
| `reports/WINDEV-04_HatchetCloud集成验证.md` | Cloud 模式离线验证结果及尚未完成的真实云验收 |
| `reports/WINDEV-05_Compose隔离与CI验证.md` | 原生语言检查与容器集成分离、本机门禁结果和远程验证限制 |
| `reports/WINDEV-06_Windows原生总验收.md` | 原生验收矩阵、实测证据、按需跳过项、限制与回滚点 |

模拟控制面总验收见 `reports/SIM-05_模拟故障注入验收报告.md`；卡住任务、Dead Letter、
Storage 清理和账本异常的恢复步骤见 `runbooks/模拟控制面故障恢复.md`。

Windows 本地工作流的生命周期、去重、恢复边界和回归结果见
`reports/WINDEV-02_LocalRunner验收.md`。

Windows 原生命令、环境加载、数据库预检和隔离库启动验证见
`reports/WINDEV-03_Windows原生命令验证.md`；用户授权后的本机开发库迁移与
原生 API/Web 就绪验证已通过。

架构决策记录位于 `docs/adr/`，依赖、模型和 custom node 的许可证台账位于
`docs/licenses/`。新增或升级社区组件时必须同时更新对应 ADR 和台账。

## 审查后的关键调整

1. **New-API 移出首版主链路。** 第一条自部署路线不需要网关；商业 API 出现后再按实际协议选择专用 Adapter 或 New-API。用户秒数始终以自有账本为准。
2. **`worker-comfyui` 只视为 RunPod／ComfyUI 基础 Worker。** 阶段三先 POC 验证 MP4 产物收集、上传、错误和冷启动，再决定薄适配范围。
3. **Hatchet 不保存业务真相。** PostgreSQL 保存 Job、Attempt 和账本；使用 Transactional Outbox 启动工作流，所有数据库及外部副作用放在幂等任务中。
4. **MVP 表大幅缩减。** 模型、节点、工作流和许可证先用不可变 manifest 管理，不提前建设模型市场式后台。
5. **Vast、SkyPilot、真实支付、完整监控和 1080p 后置。** 先把一条 720p 路线跑稳定，再依据数据扩展。
6. **FastAPI OpenAPI 是唯一接口契约。** 前端 Client 自动生成，不维护两套 DTO。

## 已删除的重复内容

- 多次重复的“不做 2K／4K、隐藏模型、ComfyUI 不开放”；
- Dify、FastGPT、A1111 不适用的长篇论证；
- 架构摘要、技术结论、不造轮子清单之间的同义内容；
- 尚未进入当前阶段的完整字段、运维指标和多云细节。
