# AI 开发规则

本文件适用于整个仓库。所有 AI 开发任务均须遵守。

## 1. 文档加载规则

默认只读取：

- `docs/00_开发总纲.md`
- 当前阶段文档

## 2. Sol 推理强度规划

推理强度由用户或运行环境在启动任务时选择；本文件不能在任务运行中自动切换强度。AI 必须先判断任务类别，并遵循以下目标档位：

| 任务类型 | 目标强度 |
| --- | --- |
| 脚手架、机械性配置、简单 CRUD、样式调整、文档和常规补测 | `light` |
| 常规功能、API 联调、普通 Bug、日常开发 | `medium` |
| 阶段拆解、数据库设计、接口契约、跨模块设计 | `high` |
| 秒数账本、双分录、幂等、并发、状态机、失败返还 | `high` |
| RunPod 超时／重试／重复回调、复杂线上故障 | `high`；无法解决时才建议 `xhigh` |
| 阶段结束前的整体架构与一致性审查 | `high`；特别复杂时使用 `xhigh` |

默认使用 `medium`。


## 3. 上下文与代理约束

- 默认不启用子代理，不自行拆出大量并行代理。
- 一个开发对话只处理一个 GitHub Issue，完成后结束该对话。
- 不在当前 Issue 范围外顺手重构、补功能或提前实现后续阶段。
- 优先用仓库文件、Issue、提交和 PR 传递状态，不依赖超长对话历史。
- 若任务过大，先拆成可独立验收的 Issues，不直接开始全量开发。

## 5. 执行与验收

每项开发任务必须：

1. 读取 Issue、开发总纲和当前阶段文档；
2. 明确本次范围与不做事项；
3. 检查现有实现后再修改；
4. 添加或更新必要测试；
5. 运行相关测试和静态检查；
6. 对照 Issue 验收标准逐项检查；
7. 报告修改、测试结果和已知限制。
没有实际测试结果时，不得宣布任务完成。架构、数据契约或阶段边界发生变化时，必须同步更新对应文档。
